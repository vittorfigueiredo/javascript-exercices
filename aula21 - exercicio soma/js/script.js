const numero1 = Number(prompt('Infome o primero número: '));
const numero2 = Number(prompt('Infome o segundo número: '));

let soma = numero1 + numero2;

alert(`A soma entre ${numero1} e ${numero2} é: ${soma}`);